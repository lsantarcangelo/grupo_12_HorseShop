<!-- Head -->
<%- include('../partials/head')  %>
<link rel="stylesheet" href="../../css/styles.css">
<link rel="stylesheet" href="../../css/header.css">
<link rel="stylesheet" href="../../css/footer.css">
<link rel="stylesheet" href="../../../css/productList.css">
</head>

<body>
    <!-- Inicio de Cabecera del Sitio -->
    <%- include('../partials/header') %>
    <!-- Inicio del Main del Sitio -->
    <main>
        <!-- Seccion tipo tarjetas con Categorias de Productos -->
        <div class="container products-wrapper">
            <div class="row">
                <div class="category-box">
                    <div class="col-12">
                        <h2 class="products-title">HOMBRES</h2>
                    </div>
                    <div class="big-box">
                        <% products.forEach((product) => { %>
                            <% if (product.category_id == 1) { %>
                                <section class="product-box">
                                    <a href="/products/detail/<%= product.id %> ">
                                    <figure class="product-box_image">
                                        <img src="/images/<%= product.product_img %>" alt="imagen de producto">
                                    </figure>
                                    <article class="product-box_data">
                                        <p><%= product.name %></p>
                                        <h2> <%= product.price %> </h2>
                                    </article>
                                    </a>
                                </section>
                            <% } %>
                        <% }) %>
                    </div>
                </div>

                <div class="category-box">
                    <div class="col-12">
                        <h2 class="products-title">MUJERES</h2>
                    </div>
                    <div class="big-box">
                        <% products.forEach((product) => { %>
                            <% if (product.category_id == 2) { %>
                                    <section class="product-box">
                                        <a href="/products/detail/<%= product.id %> ">
                                        <figure class="product-box_image">
                                            <img src="/images/<%= product.product_img %>" alt="imagen de producto">
                                        </figure>
                                        <article class="product-box_data">
                                            <h2> <%= product.price %> </h2>
                                            <p><%= product.name %></p>
                                        </article>
                                        </a>
                                    </section>
                            <% } %>
                        <% }) %>
                    </div>
                </div>
                <div class="category-box">
                    <div class="col-12">
                        <h2 class="products-title">NIÑOS</h2>
                    </div>
                    <div class="big-box">
                        <% products.forEach((product) => { %>
                            <% if (product.category_id == 3) { %>
                                <section class="product-box">
                                    <a href="/products/detail/<%= product.id %> ">
                                    <figure class="product-box_image">
                                        <img src="/images/<%= product.product_img %>" alt="imagen de producto">
                                    </figure>
                                    <article class="product-box_data">
                                        <h2> <%= product.price %> </h2>
                                        <p><%= product.name %></p>
                                    </article>
                                    </a>
                                </section>
                            <% } %>
                        <% }) %>
                    </div>
                </div>
                <div class="category-box">
                    <div class="col-12">
                        <h2 class="products-title">ACCESORIOS</h2>
                    </div>
                    <div class="big-box">
                        <% products.forEach((product) => { %>
                            <% if (product.category_id == 4) { %>
                                <section class="product-box">
                                    <a href="/products/detail/<%= product.id %> ">
                                    <figure class="product-box_image">
                                        <img src="/images/<%= product.product_img %>" alt="imagen de producto">
                                    </figure>
                                    <article class="product-box_data">
                                        <h2> <%= product.price %> </h2>
                                        <p><%= product.name %></p>
                                    </article>
                                    </a>
                                </section>
                            <% } %>
                        <% }) %>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer del Sitio -->
    <%- include('../partials/footer') %>

</body>

</html>